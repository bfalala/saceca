package Emotion_secondary;


public class matrix_effect_emotion

{
	
	/*
	 * public int[][]matrix_effect_emotion(Agent agent, Memory memory, ArrayList<String> emotions) {
	 * 
	 * ArrayList<ServiceProperty> effectList = new ArrayList<ServiceProperty>();
	 * 
	 * effectList = agent.getPossibleEffects(memory.getKnowledges()); int[][] matrice_effet_emotion
	 * = new int[effectList.size()][emotions.size()] ; int i,j=0; //for (ServiceProperty effect :
	 * effectList) { } //for (String emotion : emotions) {
	 * //if(effect.getPossible_emotion().equals(emotion)) //matrice_effet_emotion[i][j]=1; //else //
	 * matrice_effet_emotion[i][j]=1; //} //}
	 */
}
